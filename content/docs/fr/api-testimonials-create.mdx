---
title: "Creer un temoignage"
description: "Creer un nouveau temoignage via l'API NOW.TS"
keywords: ["api", "temoignages", "creation", "post"]
tags: ["api", "temoignages"]
order: 1
subcategory: "API"
method: "POST"
examples:
  bash: |
    curl -X POST https://api.nowts.com/v1/testimonials \
      -H "Authorization: Bearer YOUR_API_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{
        "content": "Amazing product! Highly recommend to everyone.",
        "author": {
          "name": "John Doe",
          "email": "john@example.com",
          "company": "Acme Corp"
        },
        "rating": 5,
        "type": "text"
      }'
  javascript: |
    const response = await fetch('https://api.nowts.com/v1/testimonials', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer YOUR_API_TOKEN',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        content: 'Amazing product! Highly recommend to everyone.',
        author: {
          name: 'John Doe',
          email: 'john@example.com',
          company: 'Acme Corp'
        },
        rating: 5,
        type: 'text'
      })
    });

    const testimonial = await response.json();
    console.log(testimonial);
  python: |
    import requests

    response = requests.post(
        'https://api.nowts.com/v1/testimonials',
        headers={
            'Authorization': 'Bearer YOUR_API_TOKEN',
            'Content-Type': 'application/json'
        },
        json={
            'content': 'Amazing product! Highly recommend to everyone.',
            'author': {
                'name': 'John Doe',
                'email': 'john@example.com',
                'company': 'Acme Corp'
            },
            'rating': 5,
            'type': 'text'
        }
    )

    testimonial = response.json()
    print(testimonial)
results:
  success: |
    {
      "id": "test_123456789",
      "content": "Amazing product! Highly recommend to everyone.",
      "author": {
        "name": "John Doe",
        "email": "john@example.com",
        "company": "Acme Corp"
      },
      "rating": 5,
      "type": "text",
      "status": "pending",
      "createdAt": "2025-08-22T10:30:00Z",
      "updatedAt": "2025-08-22T10:30:00Z"
    }
  error: |
    {
      "error": {
        "code": "validation_error",
        "message": "Invalid testimonial data",
        "details": {
          "content": "Content is required and must be at least 10 characters"
        }
      }
    }
---

Creez un nouveau temoignage dans votre organisation. Les temoignages peuvent etre au format texte, video ou audio.

## Point de terminaison

```
POST /v1/testimonials
```

## Authentification

Ce point de terminaison requiert une authentification. Incluez votre token API dans l'en-tete Authorization :

```
Authorization: Bearer YOUR_API_TOKEN
```

## Corps de la requete

| Champ            | Type   | Requis | Description                                   |
| ---------------- | ------ | ------ | --------------------------------------------- |
| `content`        | string | Oui    | Contenu du temoignage (min 10 caracteres)     |
| `author`         | object | Oui    | Informations sur l'auteur                     |
| `author.name`    | string | Oui    | Nom complet de l'auteur                       |
| `author.email`   | string | Oui    | Adresse email de l'auteur                     |
| `author.company` | string | Non    | Nom de l'entreprise de l'auteur               |
| `rating`         | number | Non    | Note de 1 a 5 etoiles                         |
| `type`           | string | Oui    | Type de temoignage : `text`, `video`, `audio` |
| `tags`           | array  | Non    | Tableau de tags pour la categorisation        |

## Reponse

Retourne un objet temoignage avec les champs suivants :

| Champ       | Type   | Description                                       |
| ----------- | ------ | ------------------------------------------------- |
| `id`        | string | Identifiant unique du temoignage                  |
| `content`   | string | Contenu du temoignage                             |
| `author`    | object | Informations sur l'auteur                         |
| `rating`    | number | Note de 1 a 5 etoiles                             |
| `type`      | string | Type de temoignage                                |
| `status`    | string | Statut actuel : `pending`, `approved`, `rejected` |
| `createdAt` | string | Timestamp ISO 8601 de creation                    |
| `updatedAt` | string | Timestamp ISO 8601 de mise a jour                 |

## Codes d'erreur

| Code  | Description                                               |
| ----- | --------------------------------------------------------- |
| `400` | Bad Request - Donnees de requete invalides                |
| `401` | Unauthorized - Token API invalide ou manquant             |
| `403` | Forbidden - Permissions insuffisantes                     |
| `422` | Validation Error - Donnees non valides                    |
| `500` | Internal Server Error - Un probleme est survenu chez nous |
